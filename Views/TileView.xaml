<catel:UserControl x:Class="Wpf2048.Views.TileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:catel="http://catel.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="4">
        <Border BorderBrush="Gray" BorderThickness="2" CornerRadius="6" Background="{Binding BackColor}">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{Binding TextColor}">
                <Run Text="{Binding Text}" FontSize="30" FontWeight="Bold" />
            </TextBlock>
        </Border>
    </Grid>
</catel:UserControl>
